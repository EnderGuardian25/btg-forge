{
  "name": "btg-forge-demo-health-app",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal zero-dependency target app for the BTG Forge demo (add a /health endpoint).",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  }
}
